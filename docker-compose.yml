services:
  app:
    build: .
    command: "./.bin/assigner"
    expose:
      - "8080"
    depends_on:
      - db
    env_file:
      - .env

  db:
    restart: always
    image: postgres:latest
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - ./db/.postgres-data:/var/lib/postgresql:Z
      - ./db/init:/docker-entrypoint-initdb.d:Z
